# mindmemory-client

Python 库与 `mmem` CLI：PNMS 长期记忆 + MindMemory `/api/v1`（含 sync 签名）。

开发安装需先安装同工作区的 `pnms`：

```bash
pip install -e ../pnms
pip install -e ".[dev]"
```

设计见 `docs/mindmemory-client-设计.md`。
